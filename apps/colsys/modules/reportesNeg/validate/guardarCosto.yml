fillin:
  enabled: true  # Enable the form repopulation
  param:
    name: formColmas  # Form name, not needed if there is only one form in the page
fields:
  netcosto:
    required:
      msg:       requerido
    sfNumberValidator:
      nan_error:    no valido
      min:          0
      min_error:    no valido
 

  vlrcosto:
    required:
      msg:       requerido
    sfNumberValidator:
      nan_error:    no valido
      min:          0
      min_error:    no valido
  
  
  mincosto:
    required:
      msg:       requerido
    sfNumberValidator:
      nan_error:    no valido
      min:          0
      min_error:    no valido
  

  
 